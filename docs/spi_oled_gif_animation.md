# ST7789 GIF 动画实现说明

## 原始素材

`小猫图.gif` 的参数为：

- 尺寸：120x120。
- 原始帧数：28。
- 原始间隔：40 ms。
- 循环时间：1120 ms。
- 带透明背景和 GIF disposal 帧处理。

Pillow 先按 GIF 规则合成每一帧，再进行颜色量化。不能直接把各帧的局部
矩形当作完整图像，否则透明区域和 disposal 处理会产生残影。

## 为什么不保存全部整帧

120x120 图像使用 RGB565 时，每帧需要 28800 字节；28 帧需要约 788 KB。
即使使用 16 色 4 位索引，每帧仍需 7200 字节，28 帧约 197 KB。

STM32F103C8 工程配置为 64 KB Flash，但当前 Keil 免费链接器还限制最终镜像
不能超过 32 KB。因此固件采用以下折中：

- 每 3 个原始帧保留 1 帧，共 10 帧。
- 播放间隔从 40 ms 合并为 120 ms。
- 全部帧共用 16 色 RGB565 调色板。
- 首帧完整保存，后续帧只保存相邻帧的变化像素。

最终首帧占 7200 字节，9 个帧差共 21099 字节。

## 帧差格式

数据按 120 个源像素一行进行编码。每段以一个控制字节开始：

```text
bit7 = 1：跳过该段，保留 ST7789 上一帧显存
bit7 = 0：绘制该段，后面跟随打包的 4 位颜色索引
bit6..0：段长度减 1，范围为 1 到 128 个源像素
```

每个绘制段会转换为一个 ST7789 地址窗口。源像素在 X、Y 方向各重复两次，
所以 120x120 数据覆盖 240x240 屏幕。未变化段不发送像素，既减少 Flash
数据，也减少 SPI 传输时间。

播放循环回到第 0 帧时会重新发送完整首帧，避免循环边界依赖上一轮状态。

## 非阻塞的含义

TIM3 每 1 ms 增加一次计数。主循环反复调用动画任务，未到 120 ms 时立即返回，
因此等待阶段不占用 CPU，可以继续加入按键、传感器或通信任务。

当前 SPI 写屏仍是同步操作：一旦开始更新某帧，函数会一直发送到该帧完成。
这是协作式非阻塞调度，不是 DMA 后台刷屏。若后续需要更高帧率，可继续改为
SPI DMA、分块传输或外部 Flash。

## 生成与校验

生成脚本为 `Tools/generate_st7789_gif.py`。它会：

1. 检查输入尺寸。
2. 生成全局 16 色调色板。
3. 量化并编码 10 帧数据。
4. 对每个帧差执行反向解码，必须与量化帧完全一致。
5. 将输入 GIF 的 SHA-256 写入头文件，便于确认数据来源。

生成的 `anim_frames.c/.h` 使用代码页 936、无 BOM，适配 ARMCC 5 工程。

## 硬件通信

```text
SPI2 SCK  -> PB13
SPI2 MOSI -> PB15
RES       -> PB10
DC        -> PB14
BLK       -> PB12
SPI Mode  -> Mode 3
```

详细的 PulseView 使用方法和历史故障复盘见
[ST7789 与逻辑分析仪排障学习指南](st7789-logic-analyzer-troubleshooting-guide.md)。
